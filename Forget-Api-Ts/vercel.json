{
  "version": 2,
  "builds": [
    {
      "src": "./index.ts",
      "use": "@vercel/node",
      "config": { 
        "includeFiles": ["dist/**/*.ts"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/"
    }
  ]
}
